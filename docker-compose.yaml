version: "3"

services: 
  nodeapp:
    image: $FULL_IMAGE_NAME:$TAG
    build: 
      context: .
    container_name: $IMAGE_NAME-$TAG
    ports: 
      - 9090-9091:8080
    networks: 
      - appnet

networks:
  appnet: